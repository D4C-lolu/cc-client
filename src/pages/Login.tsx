import { Footer, Header, LoginForm } from "../components";

const Login = () => {
  return (
    <div className="w-full bg-black">
      <Header />
      <LoginForm />
      <Footer />
    </div>
  );
};

export default Login;
